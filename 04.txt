SELECT
	video.id,
	users.first_name,
	users.last_name,
	photo.url,
	video.url,
	video.size
FROM video
JOIN users
	ON video.owner_id = users.id
JOIN photo
	ON photo.id = users.main_photo_id
ORDER BY video.size DESC
LIMIT 10
